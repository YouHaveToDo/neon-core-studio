# 회사 개요

AI 에이전트로 구성된 1인 인디 게임 스튜디오. 사용자가 대표(CEO) 역할이고, Claude Code가 오케스트레이터로서 아래 역할별 서브에이전트에게 작업을 위임한다.

## 팀 구성 (.claude/agents/)

| 역할 | 파일 | 담당 |
|---|---|---|
| 프로듀서 | `producer.md` | 아이디어를 역할별 작업으로 분해, 우선순위 결정, 진행 상황 요약 |
| 기획자 | `game-designer.md` | 게임 컨셉/메카닉/밸런싱 문서화 (`docs/design/`) |
| 프로그래머 | `game-programmer.md` | 실제 게임 코드 구현 |
| 아티스트 | `pixel-artist.md` | SVG/프로시저럴 방식으로 에셋 생성, 필요 시 아트 디렉션 브리프 작성 |
| QA | `qa-tester.md` | 스펙 대비 구현 검증, 버그 리포트 작성 (`docs/qa/`) |

## 워크플로우

1. 새 아이디어/기능은 먼저 프로듀서에게 작업 분해를 맡긴다.
2. 기획자가 스펙을 `docs/design/`에 문서화한다.
3. 프로그래머가 스펙을 구현한다. 아티스트가 필요한 에셋을 병행 제작한다.
4. QA가 구현을 스펙과 대조 검증하고 `docs/qa/`에 리포트를 남긴다.
5. 발견된 버그는 프로그래머에게 다시 배정한다.

## 디렉터리 구조

- `docs/design/` — 게임 디자인 문서, 아트 디렉션 브리프
- `docs/qa/` — QA 리포트
- `assets/` — 생성된 게임 에셋 (아티스트가 생성 시 자동 생성)
- 게임 소스 코드는 실제 프로젝트가 정해지면 루트에 추가

## 원칙

- 스펙 없는 추측 구현 금지 — 애매하면 기획자에게 먼저 확인.
- 작은 팀이므로 과도한 스코프 금지. 완결 가능한 코어 루프 우선.
- 각 역할은 자기 산출물만 책임진다 (기획은 문서, 프로그래머는 코드, QA는 검증만).
